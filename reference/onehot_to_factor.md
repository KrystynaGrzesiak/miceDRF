# One hot encoding A supplementarty function for one-hot encoding

One hot encoding

A supplementarty function for one-hot encoding

## Usage

``` r
onehot_to_factor(onehot_dat)
```

## Arguments

- onehot_dat:

  a data coded with `factor_to_onehot` function.
